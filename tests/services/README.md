# サービス層テスト (tests/services)

`src/services/` に実装されたユースケースロジックの単体テストをまとめています。インフラ依存はモック化し、サービス間の呼び出し順やデータ受け渡しを検証します。

## 主なテスト
- `test_recorder_service.py`: 録音セッション開始・停止の状態管理と戻り値を確認
- `test_processor_service.py`: 文字起こし→要約→日記保存の処理シーケンスが期待通りに呼ばれることを確認
